export const Puff = () => {
  return <div>ArticleComponent</div>
}

export default ({ name: countryCapital, capital }) => {
  // console.log(article)
  throw (new Error())

  return <div>
    <h1>{name}</h1>
    <p>{capital}</p>
  </div>
}


